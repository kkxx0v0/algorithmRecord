#include<iostream>
#include<stdlib.h>
#include<queue>
using namespace std;
int map[100005];
int bfs(int n, int k)
{
	memset(map, 0, sizeof(map));
	while (!map[k])
	{
		
	}
}
int  main()
{
	memset(map, 0, sizeof(int));
	int n, k;
	cin >> n >> k;
	cout<<bfs(n, k);
}